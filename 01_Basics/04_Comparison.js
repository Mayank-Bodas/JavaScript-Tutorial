//>
// >=
// <
// <=
// ==
// !=

// you cannot compair two different data types 

// The reason behind == and comparison is that comparison convert the null to 0 that is why null >= 0 is true and null > 0 is false

// === is the strict check it will also check the data type


